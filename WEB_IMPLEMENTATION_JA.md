# StutterSuite Web版の実装範囲

StutterSuiteは、マイク音声と原稿をこのアプリから外部サーバーへ送らない、ローカル優先のブラウザ補助です。TTSはブラウザがローカル音声として報告するvoice（`localService === true`）だけを利用し、利用できない環境では再生しません。補助の効果を保証したり、診断・治療を行ったりするものではありません。

## この版で実装しているもの

- DAF：マイク入力を枝A/Bごとに0〜300 ms遅らせ、独立したゲインと左右出力で再生します。
- マスキング・フィードバック：低い初期値のホワイト／ピンク／ブラウンノイズと、簡単な周波数調整です。
- 直接／増幅フィードバック：意図的な遅延や音程変更を加えない自己モニターです。ブラウザと機器の遅延は残ります。
- メトロノーム、原稿の句ごとのTTS、連続した斉読／TTS同期。
- 条件全体を保存するプロファイル、補助なしを含む任意の2条件比較、使用時の条件スナップショットを含む履歴、JSONバックアップ／復元。
- 対応ブラウザでのWeb Audio出力先の切替。開始前に選んだ出力先も、補助の開始時に適用します。

## Web版で実装していないもの

FAF（再生時間を変えずに行うリアルタイム音程シフト）は実装していません。FAFやDAF+FAFの操作は無効化されており、再生速度を変えて音程シフトのように見せる処理も行いません。

`speechSynthesis` のTTSはWeb Audioの出力先にルーティングできないため、OSまたはブラウザの既定出力に従います。ブラウザが`setSinkId`に未対応の場合も、補助音はOS／ブラウザの既定出力に再生されます。TTSの候補にはリモート音声サービスを含めず、`localService === true` のvoiceだけを表示します。

## 安全について

ヘッドホンで使い、最小音量から確認してください。画面上のdBFSはデジタル振幅であり、耳元の音圧（SPL）ではありません。緊急停止は自己フィードバック、ノイズ、メトロノーム、TTSを止めてマイクを解放します。通話アプリのミュートは別途操作してください。
